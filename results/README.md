# Results

This directory contains compact CSV and Markdown summaries generated by the validation scripts.

The result files are small enough to version and are included to make the paper claims auditable without forcing every user to rerun the full experiment suite. Regenerating the scripts may overwrite these files.
